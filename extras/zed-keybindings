{
  "context": "Editor",
  "bindings": {
    // These are the bindings for Ubuntu with kinto.sh; if you move this to a Mac replace ctrl with cmd
    // changes the 'swap tab' behavior to the 'usable' command-key kind instead of ctrl (on a Mac especially the default feels weird as hell)
    "ctrl-1": ["pane::ActivateItem", 0],
    "ctrl-2": ["pane::ActivateItem", 1],
    "ctrl-3": ["pane::ActivateItem", 2],
    "ctrl-4": ["pane::ActivateItem", 3],
    "ctrl-5": ["pane::ActivateItem", 4],
    "ctrl-6": ["pane::ActivateItem", 5],
    "ctrl-7": ["pane::ActivateItem", 6],
    "ctrl-8": ["pane::ActivateItem", 7],
    "ctrl-9": ["pane::ActivateItem", 8],
    "ctrl-0": ["pane::ActivateItem", 9],
    "ctrl-w": "pane::CloseActiveItem"
  }
}
